{% extends "base.html" %}

{% block content %}

{% for post in object_list %}

<h2><a href="/market/{{ ad.id }}">{{ ad.title }}</a></h2>

<div class="post_meta">
	{{ ad.created }}
</div>

<div class="post_body">
	{{ ad.body|safe|linebreaks }}
</div>

{% endfor %}

{% endblock %}
